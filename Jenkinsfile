pipeline {
    agent any 
    stages {
        stage('SCM') {
            steps {
                checkout scm 
            }
        }

        //stage('HOLAMUNDO') {
        //    steps {
        //        sh 'Hola Mundo'
        //    }
       // }
    }
}